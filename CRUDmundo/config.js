/**
 * Configuração global do front-end.
 * Ajuste API_BASE_URL conforme o local onde a pasta backend/ foi publicada
 * (por padrão, backend/ e frontend/ ficam lado a lado no mesmo host).
 */
const API_BASE_URL = '../backend/api';
